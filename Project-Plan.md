下面这版可以直接分享给组员。
我会把目标改成：**先完成课程大作业交付，再考虑 GitHub 优化**。所以计划里不会把 README、CI、开源规范放在主线，而是放到最后作为补充。

---

# DDLRadar 项目分工表与五周计划表

## 一、项目定位

项目名称：**DDLRadar**

项目简介：

> DDLRadar 是一个面向学生的课程 DDL 管理系统，用于记录作业、考试、课程项目等任务，并根据截止时间、优先级、完成状态自动计算紧急程度。同时提供简单的 AI 辅助功能，帮助用户拆解任务、生成复习建议或整理待办计划。

项目目标：

```text
第一目标：完成软件工程课程大作业要求，保证功能完整、文档齐全、能正常演示。

第二目标：如果时间允许，再进一步整理代码结构、完善 README，并上传 GitHub。
```

---

# 二、项目核心功能范围

## 1. 必做功能

| 功能模块      | 说明                       |
| --------- | ------------------------ |
| DDL 管理    | 新增、查看、修改、删除课程任务          |
| 任务分类      | 支持作业、考试、课程项目、其他任务        |
| 截止时间管理    | 记录 deadline，支持按时间排序      |
| 状态管理      | 未开始、进行中、已完成、已逾期          |
| 优先级管理     | 低、中、高优先级                 |
| 紧急程度计算    | 根据截止时间、优先级、完成状态生成风险等级    |
| Dashboard | 展示近期 DDL、逾期任务、高风险任务、任务统计 |
| AI 辅助     | 根据任务信息生成拆解建议、复习建议或完成计划   |
| 数据持久化     | 使用 SQLite 保存任务数据         |
| 测试与文档     | 完成测试报告、需求文档、设计文档、AI 使用说明 |

---

## 2. 暂不作为主线的功能

这些功能可以先不做，避免项目范围过大。

| 功能         | 原因                |
| ---------- | ----------------- |
| 登录注册       | 课程项目中不是核心，容易增加复杂度 |
| 多用户权限      | 会明显增加后端和数据库复杂度    |
| 微信/邮件提醒    | 接入成本较高，演示时不稳定     |
| 手机 App     | 五周内投入产出比不高        |
| 复杂日历同步     | 接口和授权流程复杂         |
| AI 自动全流程规划 | 容易做得太虚，建议只做辅助建议生成 |

---

# 三、推荐技术路线

## 主线技术栈

| 部分      | 技术                      |
| ------- | ----------------------- |
| 后端语言    | Rust                    |
| Web 框架  | Axum                    |
| 数据库     | SQLite                  |
| 数据库访问   | rusqlite 或 sqlx         |
| 时间处理    | chrono                  |
| JSON 处理 | serde / serde_json      |
| 前端      | HTML + CSS + JavaScript |
| AI 接入   | 先做 Mock 模式，再接入真实 API    |
| 文档      | Markdown                |

---

# 四、系统模块划分

建议项目按下面模块设计：

```text
DDLRadar
├── 任务管理模块
│   ├── 新增任务
│   ├── 查询任务
│   ├── 修改任务
│   └── 删除任务
│
├── 风险计算模块
│   ├── 计算剩余时间
│   ├── 判断是否逾期
│   ├── 结合优先级计算风险等级
│   └── 输出风险标签
│
├── AI 辅助模块
│   ├── 任务拆解建议
│   ├── 复习计划建议
│   ├── 一周 DDL 总结
│   └── AI 使用记录
│
├── 数据库模块
│   ├── 保存任务
│   ├── 更新任务
│   ├── 删除任务
│   └── 查询统计数据
│
├── Web 接口模块
│   ├── REST API
│   ├── Dashboard 数据接口
│   └── AI 建议接口
│
└── 前端展示模块
    ├── 任务列表
    ├── 新增任务表单
    ├── DDL 风险面板
    └── AI 建议展示区域
```

---

# 五、角色分工表

为了便于沟通，暂时用 A、B、C、D 表示四位成员。

---

## A：项目统筹、需求设计、核心规则与文档整合

### A 需要先了解的内容

| 内容            | 需要掌握到什么程度                         |
| ------------- | --------------------------------- |
| Rust 基础语法     | 能看懂 struct、enum、Vec、Option、Result |
| serde         | 知道 Rust 结构体如何转 JSON               |
| chrono        | 了解如何处理日期和时间                       |
| REST API 基本概念 | 知道 GET、POST、PUT、DELETE 分别做什么      |
| 软件工程文档结构      | 能写需求、设计、测试、AI 使用说明                |
| DDLRadar 业务逻辑 | 能说清系统要解决什么问题、第一版做什么、不做什么          |

### A 的主要任务

| 阶段   | 具体任务                                       |
| ---- | ------------------------------------------ |
| 项目初期 | 明确项目范围，防止功能膨胀                              |
| 项目初期 | 编写需求文档初稿                                   |
| 项目初期 | 设计核心数据模型，例如 Task、Priority、Status、RiskLevel |
| 项目初期 | 制定 API 草案，方便 B、C、D 并行开发                    |
| 中期   | 设计紧急程度计算规则                                 |
| 中期   | 协助检查后端接口是否符合需求                             |
| 中期   | 整理 AI 使用记录模板                               |
| 后期   | 整合需求文档、设计文档、测试报告、AI 使用说明                   |
| 后期   | 准备答辩演示流程和讲解稿                               |

### A 需要完成的具体产物

```text
1. docs/需求文档.md
2. docs/概要设计文档.md
3. docs/API接口草案.md
4. docs/AI使用说明.md
5. 风险等级计算规则说明
6. 项目最终汇报 PPT 内容提纲
7. 演示流程说明
```

### A 是否需要熟练掌握 Rust？

**不需要一开始就熟练掌握 Rust。**

A 更重要的是掌握：

```text
1. Rust 项目怎么运行
2. Rust 数据结构怎么定义
3. 后端接口大概怎么组织
4. 业务逻辑怎么拆分
5. 文档和演示怎么落地
```

A 不需要深入掌握：

```text
1. 生命周期高级用法
2. 宏
3. 多线程
4. 高级泛型
5. unsafe Rust
6. 复杂异步原理
```

但是 A 至少要能做到：

```text
1. 能运行 cargo run
2. 能运行 cargo test
3. 能看懂主要结构体
4. 能理解接口输入输出
5. 能修改简单业务规则
```

---

## B：后端接口与数据库模块

### B 需要先了解的内容

| 内容              | 需要掌握到什么程度       |
| --------------- | --------------- |
| Axum            | 能写基础路由和 handler |
| tokio           | 知道异步 main 函数怎么写 |
| SQLite          | 能设计基础表结构        |
| rusqlite 或 sqlx | 能完成增删改查         |
| serde           | 能处理请求和响应 JSON   |
| REST API        | 能根据接口文档实现接口     |

### B 的主要任务

| 阶段   | 具体任务                    |
| ---- | ----------------------- |
| 项目初期 | 搭建后端 Axum 项目            |
| 项目初期 | 实现 `/api/health` 健康检查接口 |
| 项目初期 | 根据 A 的数据模型设计数据库表        |
| 中期   | 实现任务 CRUD 接口            |
| 中期   | 实现任务查询、排序、筛选接口          |
| 中期   | 实现 Dashboard 数据接口       |
| 后期   | 对接 C 提供的风险计算和 AI 服务函数   |
| 后期   | 协助完成接口测试                |

### B 需要完成的具体产物

```text
1. 后端服务可运行
2. SQLite 数据库表
3. 任务新增接口
4. 任务查询接口
5. 任务修改接口
6. 任务删除接口
7. Dashboard 统计接口
8. API 测试记录
```

### B 负责的主要接口示例

```text
GET    /api/health
GET    /api/tasks
POST   /api/tasks
PUT    /api/tasks/:id
DELETE /api/tasks/:id
GET    /api/dashboard
POST   /api/ai/suggest
```

---

## C：风险计算、AI 辅助模块与业务服务层

### C 需要先了解的内容

| 内容         | 需要掌握到什么程度                  |
| ---------- | -------------------------- |
| Rust 函数与模块 | 能把业务逻辑拆成独立函数               |
| chrono     | 能计算当前时间和 deadline 的差值      |
| enum       | 能定义风险等级                    |
| reqwest    | 如果接真实 AI API，需要会发送 HTTP 请求 |
| Prompt 设计  | 能写清楚 AI 输入内容和输出格式          |
| 错误处理       | 能处理 AI 接口失败、超时、无返回等情况      |

### C 的主要任务

| 阶段   | 具体任务                          |
| ---- | ----------------------------- |
| 项目初期 | 根据 A 的规则实现风险计算函数              |
| 项目初期 | 定义 RiskLevel：低风险、中风险、高风险、已逾期  |
| 中期   | 实现根据任务列表生成统计数据                |
| 中期   | 设计 AI 提示词模板                   |
| 中期   | 实现 Mock AI 模式，保证没有真实 API 也能演示 |
| 中期   | 接入真实 AI API，作为增强功能            |
| 后期   | 为 AI 模块添加异常处理                 |
| 后期   | 整理 AI 模块说明，交给 A 写入文档          |

### C 需要完成的具体产物

```text
1. 风险等级计算函数
2. 任务统计函数
3. AI Prompt 模板
4. Mock AI 返回逻辑
5. 真实 AI API 接入代码
6. AI 接口失败时的兜底提示
7. AI 模块测试用例
```

---

## D：前端页面、测试与辅助文档

### D 需要先了解的内容

| 内容               | 需要掌握到什么程度      |
| ---------------- | -------------- |
| HTML/CSS         | 能写基础页面和表单      |
| JavaScript fetch | 能调用后端 API      |
| JSON             | 能理解接口返回数据      |
| 表格和卡片布局          | 能展示任务列表和统计信息   |
| 手动测试方法           | 能按照测试用例验证功能    |
| Markdown         | 能整理测试报告和用户使用说明 |

### D 的主要任务

| 阶段   | 具体任务              |
| ---- | ----------------- |
| 项目初期 | 设计前端页面草图          |
| 项目初期 | 编写基础 HTML 页面      |
| 中期   | 实现任务列表展示          |
| 中期   | 实现新增任务表单          |
| 中期   | 实现任务状态修改按钮        |
| 中期   | 展示 Dashboard 统计信息 |
| 中期   | 展示 AI 建议结果        |
| 后期   | 编写测试用例            |
| 后期   | 整理测试报告和用户使用说明     |

### D 需要完成的具体产物

```text
1. 首页 Dashboard 页面
2. DDL 任务列表页面
3. 新增任务表单
4. AI 建议展示区域
5. 功能测试用例表
6. 测试报告
7. 用户使用说明
8. 演示截图
```

---

# 六、AI 接入功能设计

AI 功能不要做得太大，建议定位为：

> **AI 不是系统核心，而是辅助用户理解和拆解 DDL。**

这样比较容易交付，也方便在文档中说明。

---

## 1. AI 功能一：任务拆解建议

用户选择一个 DDL，例如：

```text
软件工程课程大作业
截止时间：2026-06-20
优先级：高
当前状态：未开始
```

AI 返回：

```text
建议拆分为：
1. 确定项目需求
2. 完成数据库设计
3. 实现任务管理接口
4. 实现前端页面
5. 编写测试报告
6. 准备答辩材料
```

---

## 2. AI 功能二：复习计划建议

适用于考试类任务。

例如任务类型为：

```text
高等数学期末考试
截止时间：2026-06-25
优先级：高
```

AI 返回：

```text
建议复习安排：
第 1 天：复习极限与连续
第 2 天：复习导数与微分
第 3 天：复习积分
第 4 天：刷综合题
第 5 天：整理错题
```

---

## 3. AI 功能三：一周 DDL 总结

系统把未来 7 天内的 DDL 发给 AI。

AI 返回：

```text
本周任务较集中，建议优先处理软件工程课程大作业和数据库实验。
其中软件工程大作业截止时间最近，且优先级较高，应尽快开始。
```

---

## 4. AI 接入方式

建议分两层实现。

### 第一层：Mock AI 模式，必做

不调用真实 API，直接根据任务信息返回模板化建议。

好处：

```text
1. 不依赖网络
2. 不需要 API Key
3. 演示稳定
4. 方便测试
5. 一定能交付
```

示例：

```text
如果任务类型是“课程项目”：
返回项目拆解模板。

如果任务类型是“考试”：
返回复习计划模板。

如果任务类型是“作业”：
返回完成步骤模板。
```

### 第二层：真实 AI API，选做增强

如果时间允许，可以接入真实 AI 服务。

建议保留统一接口：

```rust
trait AiAdvisor {
    fn generate_advice(&self, task: &Task) -> Result<String, AiError>;
}
```

然后实现两个版本：

```text
MockAiAdvisor
真实 AiApiAdvisor
```

这样即使真实 AI 接口出问题，系统也能切回 Mock 模式。

---

# 七、AI 使用说明文档要求

课程文档中建议单独写一个：

```text
docs/AI使用说明.md
```

内容包括：

## 1. 项目中 AI 功能的使用

写清楚系统中 AI 具体做了什么：

```text
1. 根据 DDL 信息生成任务拆解建议
2. 根据考试类任务生成复习计划
3. 根据未来 7 天任务生成 DDL 总结
```

---

## 2. AI 不负责的内容

要写清楚边界：

```text
1. AI 不直接修改用户任务
2. AI 不自动决定任务优先级
3. AI 生成内容仅作为参考
4. 最终计划由用户自行确认
```

---

## 3. 开发过程中 AI 的使用记录

建议用表格记录：

| 日期         | 成员 | 使用阶段  | 使用内容           | 人工修改情况    |
| ---------- | -- | ----- | -------------- | --------- |
| 2026.xx.xx | A  | 需求分析  | 辅助整理项目功能范围     | 已人工筛选和修改  |
| 2026.xx.xx | B  | 后端开发  | 辅助生成 Axum 接口示例 | 已根据项目代码修改 |
| 2026.xx.xx | C  | AI 模块 | 辅助设计 Prompt 模板 | 已人工调整     |
| 2026.xx.xx | D  | 测试文档  | 辅助整理测试用例       | 已人工验证     |

---

## 4. AI 使用原则

建议写成：

```text
本项目在开发过程中使用 AI 工具辅助需求分析、代码示例生成、文档整理和测试用例设计。
所有 AI 生成内容均经过人工检查、修改和验证。
AI 不作为最终决策者，项目功能设计、代码整合和测试结果均由小组成员确认。
```

---

# 八、五周项目计划表

## 第 1 周：需求确定与项目骨架

目标：确定做什么、不做什么，并搭建最小项目结构。

| 成员 | 任务                                        |
| -- | ----------------------------------------- |
| A  | 编写需求文档初稿，确定 MVP 功能范围                      |
| A  | 设计 Task、Priority、Status、RiskLevel 等核心数据结构 |
| A  | 编写 API 草案                                 |
| B  | 搭建 Rust 后端项目，跑通 Axum hello world          |
| B  | 设计 SQLite 数据库表                            |
| C  | 设计风险等级计算规则                                |
| C  | 设计 AI 功能范围和 Prompt 初稿                     |
| D  | 设计前端页面草图                                  |
| D  | 编写基础 HTML 页面                              |

第 1 周交付物：

```text
1. 需求文档初稿
2. API 草案
3. 数据库表设计
4. Rust 后端项目可运行
5. 前端页面草图
6. AI 功能设计初稿
```

---

## 第 2 周：任务管理主功能开发

目标：完成 DDL 任务的基本增删改查。

| 成员 | 任务            |
| -- | ------------- |
| A  | 跟进接口和数据模型是否一致 |
| A  | 补充概要设计文档      |
| B  | 实现任务新增接口      |
| B  | 实现任务查询接口      |
| B  | 实现任务修改接口      |
| B  | 实现任务删除接口      |
| C  | 实现风险等级计算函数    |
| C  | 实现任务排序和筛选逻辑   |
| D  | 实现任务列表页面      |
| D  | 实现新增任务表单      |
| D  | 调用后端接口展示任务数据  |

第 2 周交付物：

```text
1. DDL 可以新增
2. DDL 可以查看
3. DDL 可以修改
4. DDL 可以删除
5. 前端可以展示任务列表
6. 风险等级可以初步计算
```

---

## 第 3 周：Dashboard 与 AI 辅助功能

目标：完成可展示的亮点功能。

| 成员 | 任务                      |
| -- | ----------------------- |
| A  | 整理 AI 使用说明文档框架          |
| A  | 检查 Dashboard 展示内容是否符合需求 |
| B  | 实现 Dashboard 统计接口       |
| B  | 实现 AI 建议接口入口            |
| C  | 完成 Mock AI 模式           |
| C  | 实现任务拆解建议                |
| C  | 实现复习计划建议                |
| C  | 视情况接入真实 AI API          |
| D  | 实现 Dashboard 页面         |
| D  | 实现 AI 建议展示区域            |
| D  | 完成基础交互优化                |

第 3 周交付物：

```text
1. Dashboard 可以展示任务统计
2. 可以看到高风险任务
3. 可以看到逾期任务
4. AI 可以生成任务拆解建议
5. AI 可以生成复习计划建议
6. 前端可以展示 AI 返回内容
```

---

## 第 4 周：测试、修复与课程文档

目标：把项目从“能跑”变成“能交”。

| 成员 | 任务                  |
| -- | ------------------- |
| A  | 整合需求文档、设计文档、AI 使用说明 |
| A  | 编写演示流程              |
| B  | 修复后端接口问题            |
| B  | 补充后端接口测试            |
| C  | 补充风险计算和 AI 模块测试     |
| C  | 完善 AI 失败兜底逻辑        |
| D  | 编写测试用例              |
| D  | 完成功能测试              |
| D  | 整理测试报告和用户使用说明       |

第 4 周交付物：

```text
1. 系统功能基本稳定
2. 主要接口可正常运行
3. 前端页面可完整演示
4. 测试报告完成
5. AI 使用说明完成
6. 需求和设计文档基本完成
```

---

## 第 5 周：最终打磨与答辩准备

目标：保证演示稳定，文档齐全。

| 成员 | 任务               |
| -- | ---------------- |
| A  | 整合最终文档           |
| A  | 准备汇报 PPT 和讲解稿    |
| B  | 检查后端运行稳定性        |
| B  | 整理接口说明           |
| C  | 检查风险计算和 AI 模块稳定性 |
| C  | 准备 AI 功能演示示例     |
| D  | 准备演示截图           |
| D  | 完善测试报告           |
| D  | 协助准备答辩材料         |

第 5 周交付物：

```text
1. 可演示系统
2. 完整课程文档
3. 测试报告
4. AI 使用说明
5. 答辩 PPT
6. 演示脚本
```

---

# 九、A 的任务是否太轻？

从表面看，A 的代码任务可能没有 B、C 多，但 A 的任务并不轻。

A 的重点不是“写最多代码”，而是负责：

```text
1. 控制项目范围
2. 统一数据模型
3. 统一接口设计
4. 整合文档
5. 保证最后能交
6. 准备答辩
7. 发现进度风险
8. 协调模块之间的衔接
```

如果 A 只做需求文档，那确实偏轻。
但如果 A 同时负责：

```text
1. 需求文档
2. 概要设计
3. API 草案
4. 风险规则设计
5. AI 使用说明
6. 最终文档整合
7. 答辩材料
8. 每周进度检查
```

那么 A 的任务量是合理的。

比较适合 A 的投入方式是：

```text
平时每天 1～2 小时；
每周固定一次集中整理和检查；
第 4～5 周适当增加文档整合和答辩准备时间。
```

---

# 十、降低协作成本的接口约定

为了让不同成员可以并行开发，建议尽早固定几个接口。

## 1. 任务数据结构

```json
{
  "id": 1,
  "title": "软件工程课程大作业",
  "course": "软件工程",
  "task_type": "project",
  "deadline": "2026-06-20 23:59",
  "priority": "high",
  "status": "todo",
  "description": "完成 DDLRadar 项目",
  "risk_level": "high"
}
```

---

## 2. AI 建议接口

```text
POST /api/ai/suggest
```

请求：

```json
{
  "title": "软件工程课程大作业",
  "course": "软件工程",
  "task_type": "project",
  "deadline": "2026-06-20 23:59",
  "priority": "high",
  "status": "todo",
  "description": "完成 DDLRadar 项目"
}
```

响应：

```json
{
  "advice": "建议先完成需求分析，再进行数据库设计、后端接口开发、前端页面开发和测试报告整理。"
}
```

---

## 3. Dashboard 统计接口

```text
GET /api/dashboard
```

响应：

```json
{
  "total_tasks": 12,
  "todo_tasks": 7,
  "doing_tasks": 3,
  "done_tasks": 2,
  "overdue_tasks": 1,
  "high_risk_tasks": 4
}
```

这些接口一旦确定，各成员就可以减少反复沟通，按接口并行开发。

---

# 十一、主线完成后的 GitHub 补充任务

如果后续想要上传到 GitHub，可以在主线完成后补充这些内容。

## 1. README 优化

补充：

```text
1. 项目简介
2. 功能列表
3. 技术栈
4. 启动方式
5. 页面截图
6. API 示例
7. 小组分工
```

---

## 2. 代码结构整理

补充：

```text
1. 删除无用代码
2. 统一命名风格
3. 整理 src 目录
4. 把业务逻辑拆成模块
5. 添加必要注释
```

---

## 3. GitHub Actions

可选添加：

```text
cargo fmt
cargo clippy
cargo test
```

---

## 4. 示例数据

补充一组演示数据：

```text
1. 软件工程大作业
2. 高等数学期末考试
3. 数据库实验报告
4. 英语口语展示
5. 操作系统作业
```

这样别人 clone 下来后能直接看到效果。

---

## 5. 项目截图和演示 GIF

可以放在：

```text
docs/images/
```

用于展示：

```text
1. Dashboard 页面
2. 任务列表页面
3. 新增任务页面
4. AI 建议页面
```

---

# 十二、最终建议

这个项目的优先级应该是：

```text
第一优先级：
能新增、查看、修改、删除 DDL。

第二优先级：
能计算风险等级，能展示 Dashboard。

第三优先级：
能演示 AI 辅助建议，并写清楚 AI 使用说明。

第四优先级：
文档完整，测试报告完整，答辩能讲清楚。

第五优先级：
整理 GitHub、README、CI、开源规范。
```

也就是说，DDLRadar 不要一开始就追求“像商业产品”，而是先做到：

> **功能闭环清楚、Rust 代码能跑、AI 亮点能演示、文档能交、答辩能讲。**

这样最稳。
